//WAP to find user enter is vowel or not
//char
#include<stdio.h>
void main ()
{
    char alphabet;
    printf("Enter the letter : ");
    scanf("%c",&alphabet);
    if(alphabet=='a')
        {
            printf("It is vowel");
        }
        else if(alphabet=='e')
        {
            printf("It is vowel");
        }
        else if(alphabet=='i')
        {
            printf("It is vowel");
        }
        else if(alphabet=='o')
        {
            printf("It is vowel");
        }
        else if(alphabet=='u')
        {
            printf("It is vowel");
        }
        else if(alphabet=='A')
        {
            printf("It is vowel");
        }
        else if(alphabet=='E')
        {
            printf("It is vowel");
        }
        else if(alphabet=='I')
        {
            printf("It is vowel");
        }
        else if(alphabet=='O')
        {
            printf("It is vowel");
        }
        else if(alphabet=='U')
        {
            printf("It is vowel");
        }
    else
        {
            printf("it is not vowel");
        }
}